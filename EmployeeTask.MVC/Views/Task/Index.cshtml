@using EmployeeTask.Models.Entities.TaskModels;
@model List<TaskViewModel>;


<h1>Tasks:</h1>
@if(Model.Count == 0)
{
	<h1>No tasks found</h1>
}
@foreach(var obj in Model)
{
    <p>@obj.Title</p>
	<div>
		@foreach(var a in obj.Assignees)
		{
			<p>@a.FullName</p>
		}
	</div>
}
